name: Order Service CI

on:
  push:
    paths:
      - "services/order_service/**"
  pull_request:
    paths:
      - "services/order_service/**"

jobs:
  ci:
    uses: ./.github/workflows/rails-ci.yml
    with:
      service-path: services/order_service